{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test:dev": "cd ../ && cross-env testEnv=dev mocha -R mochawesome -t 0 --ignore test/util.js --reporter-options reportDir=test/res,reportFilename=index",
    "test:build": "cd ../ && cross-env testEnv=build mocha -R mochawesome -t 0 --ignore test/util.js --reporter-options reportDir=test/res,reportFilename=index"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "axios": "^0.21.1",
    "mocha": "^9.0.2",
    "mochawesome": "^6.2.2",
    "shelljs": "^0.8.4",
    "ws": "^7.5.3"
  }
}
