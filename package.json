{
  "scripts": {
    "build": "cd client && npm run build && cd ../release && npx gulp",
    "bt": "npm run build && npm run test",
    "test": "cd dist/package && cnpm i && cd ../../ && node dist/package/run.js"
  }
}
